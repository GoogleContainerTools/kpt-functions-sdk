
/**
 * CODE GENERATED BY 'typgen' BINARY.
 *
 * DO NOT EDIT.
 */

import { KubernetesObject } from 'kpt-functions';
import * as apisMetaV1 from './io.k8s.apimachinery.pkg.apis.meta.v1';

// FlowDistinguisherMethod specifies the method of a flow distinguisher.
export class FlowDistinguisherMethod {
  // `type` is the type of flow distinguisher method The supported types are "ByUser" and "ByNamespace". Required.
  public type: string;

  constructor(desc: FlowDistinguisherMethod) {
    this.type = desc.type;
  }
}

// FlowSchema defines the schema of a group of flows. Note that a flow is made up of a set of inbound API requests with similar attributes and is identified by a pair of strings: the name of the FlowSchema and a "flow distinguisher".
export class FlowSchema implements KubernetesObject {
  // APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
  public apiVersion: string;

  // Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
  public kind: string;

  // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
  public metadata: apisMetaV1.ObjectMeta;

  // `spec` is the specification of the desired behavior of a FlowSchema. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
  public spec?: FlowSchemaSpec;

  // `status` is the current status of a FlowSchema. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
  public status?: FlowSchemaStatus;

  constructor(desc: FlowSchema.Interface) {
    this.apiVersion = FlowSchema.apiVersion;
    this.kind = FlowSchema.kind;
    this.metadata = desc.metadata;
    this.spec = desc.spec;
    this.status = desc.status;
  }
}

export function isFlowSchema(o: any): o is FlowSchema {
  return o && o.apiVersion === FlowSchema.apiVersion && o.kind === FlowSchema.kind;
}

export namespace FlowSchema {
  export const apiVersion = "flowcontrol.apiserver.k8s.io/v1beta1";
  export const group = "flowcontrol.apiserver.k8s.io";
  export const version = "v1beta1";
  export const kind = "FlowSchema";

  // named constructs a FlowSchema with metadata.name set to name.
  export function named(name: string): FlowSchema {
    return new FlowSchema({metadata: {name}});
  }
  // FlowSchema defines the schema of a group of flows. Note that a flow is made up of a set of inbound API requests with similar attributes and is identified by a pair of strings: the name of the FlowSchema and a "flow distinguisher".
  export interface Interface {
    // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
    metadata: apisMetaV1.ObjectMeta;

    // `spec` is the specification of the desired behavior of a FlowSchema. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
    spec?: FlowSchemaSpec;

    // `status` is the current status of a FlowSchema. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
    status?: FlowSchemaStatus;
  }
}

// FlowSchemaCondition describes conditions for a FlowSchema.
export class FlowSchemaCondition {
  // `lastTransitionTime` is the last time the condition transitioned from one status to another.
  public lastTransitionTime?: apisMetaV1.Time;

  // `message` is a human-readable message indicating details about last transition.
  public message?: string;

  // `reason` is a unique, one-word, CamelCase reason for the condition's last transition.
  public reason?: string;

  // `status` is the status of the condition. Can be True, False, Unknown. Required.
  public status?: string;

  // `type` is the type of the condition. Required.
  public type?: string;
}

// FlowSchemaList is a list of FlowSchema objects.
export class FlowSchemaList {
  // APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
  public apiVersion: string;

  // `items` is a list of FlowSchemas.
  public items: FlowSchema[];

  // Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
  public kind: string;

  // `metadata` is the standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
  public metadata?: apisMetaV1.ListMeta;

  constructor(desc: FlowSchemaList) {
    this.apiVersion = FlowSchemaList.apiVersion;
    this.items = desc.items.map((i) => new FlowSchema(i));
    this.kind = FlowSchemaList.kind;
    this.metadata = desc.metadata;
  }
}

export function isFlowSchemaList(o: any): o is FlowSchemaList {
  return o && o.apiVersion === FlowSchemaList.apiVersion && o.kind === FlowSchemaList.kind;
}

export namespace FlowSchemaList {
  export const apiVersion = "flowcontrol.apiserver.k8s.io/v1beta1";
  export const group = "flowcontrol.apiserver.k8s.io";
  export const version = "v1beta1";
  export const kind = "FlowSchemaList";

  // FlowSchemaList is a list of FlowSchema objects.
  export interface Interface {
    // `items` is a list of FlowSchemas.
    items: FlowSchema[];

    // `metadata` is the standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
    metadata?: apisMetaV1.ListMeta;
  }
}

// FlowSchemaSpec describes how the FlowSchema's specification looks like.
export class FlowSchemaSpec {
  // `distinguisherMethod` defines how to compute the flow distinguisher for requests that match this schema. `nil` specifies that the distinguisher is disabled and thus will always be the empty string.
  public distinguisherMethod?: FlowDistinguisherMethod;

  // `matchingPrecedence` is used to choose among the FlowSchemas that match a given request. The chosen FlowSchema is among those with the numerically lowest (which we take to be logically highest) MatchingPrecedence.  Each MatchingPrecedence value must be ranged in [1,10000]. Note that if the precedence is not specified, it will be set to 1000 as default.
  public matchingPrecedence?: number;

  // `priorityLevelConfiguration` should reference a PriorityLevelConfiguration in the cluster. If the reference cannot be resolved, the FlowSchema will be ignored and marked as invalid in its status. Required.
  public priorityLevelConfiguration: PriorityLevelConfigurationReference;

  // `rules` describes which requests will match this flow schema. This FlowSchema matches a request if and only if at least one member of rules matches the request. if it is an empty slice, there will be no requests matching the FlowSchema.
  public rules?: PolicyRulesWithSubjects[];

  constructor(desc: FlowSchemaSpec) {
    this.distinguisherMethod = desc.distinguisherMethod;
    this.matchingPrecedence = desc.matchingPrecedence;
    this.priorityLevelConfiguration = desc.priorityLevelConfiguration;
    this.rules = desc.rules;
  }
}

// FlowSchemaStatus represents the current state of a FlowSchema.
export class FlowSchemaStatus {
  // `conditions` is a list of the current states of FlowSchema.
  public conditions?: FlowSchemaCondition[];
}

// GroupSubject holds detailed information for group-kind subject.
export class GroupSubject {
  // name is the user group that matches, or "*" to match all user groups. See https://github.com/kubernetes/apiserver/blob/master/pkg/authentication/user/user.go for some well-known group names. Required.
  public name: string;

  constructor(desc: GroupSubject) {
    this.name = desc.name;
  }
}

// LimitResponse defines how to handle requests that can not be executed right now.
export class LimitResponse {
  // `queuing` holds the configuration parameters for queuing. This field may be non-empty only if `type` is `"Queue"`.
  public queuing?: QueuingConfiguration;

  // `type` is "Queue" or "Reject". "Queue" means that requests that can not be executed upon arrival are held in a queue until they can be executed or a queuing limit is reached. "Reject" means that requests that can not be executed upon arrival are rejected. Required.
  public type: string;

  constructor(desc: LimitResponse) {
    this.queuing = desc.queuing;
    this.type = desc.type;
  }
}

// LimitedPriorityLevelConfiguration specifies how to handle requests that are subject to limits. It addresses two issues:
//  * How are requests for this priority level limited?
//  * What should be done with requests that exceed the limit?
export class LimitedPriorityLevelConfiguration {
  // `assuredConcurrencyShares` (ACS) configures the execution limit, which is a limit on the number of requests of this priority level that may be exeucting at a given time.  ACS must be a positive number. The server's concurrency limit (SCL) is divided among the concurrency-controlled priority levels in proportion to their assured concurrency shares. This produces the assured concurrency value (ACV) --- the number of requests that may be executing at a time --- for each such priority level:
  // 
  //             ACV(l) = ceil( SCL * ACS(l) / ( sum[priority levels k] ACS(k) ) )
  // 
  // bigger numbers of ACS mean more reserved concurrent requests (at the expense of every other PL). This field has a default value of 30.
  public assuredConcurrencyShares?: number;

  // `limitResponse` indicates what to do with requests that can not be executed right now
  public limitResponse?: LimitResponse;
}

// NonResourcePolicyRule is a predicate that matches non-resource requests according to their verb and the target non-resource URL. A NonResourcePolicyRule matches a request if and only if both (a) at least one member of verbs matches the request and (b) at least one member of nonResourceURLs matches the request.
export class NonResourcePolicyRule {
  // `nonResourceURLs` is a set of url prefixes that a user should have access to and may not be empty. For example:
  //   - "/healthz" is legal
  //   - "/hea*" is illegal
  //   - "/hea" is legal but matches nothing
  //   - "/hea/*" also matches nothing
  //   - "/healthz/*" matches all per-component health checks.
  // "*" matches all non-resource urls. if it is present, it must be the only entry. Required.
  public nonResourceURLs: string[];

  // `verbs` is a list of matching verbs and may not be empty. "*" matches all verbs. If it is present, it must be the only entry. Required.
  public verbs: string[];

  constructor(desc: NonResourcePolicyRule) {
    this.nonResourceURLs = desc.nonResourceURLs;
    this.verbs = desc.verbs;
  }
}

// PolicyRulesWithSubjects prescribes a test that applies to a request to an apiserver. The test considers the subject making the request, the verb being requested, and the resource to be acted upon. This PolicyRulesWithSubjects matches a request if and only if both (a) at least one member of subjects matches the request and (b) at least one member of resourceRules or nonResourceRules matches the request.
export class PolicyRulesWithSubjects {
  // `nonResourceRules` is a list of NonResourcePolicyRules that identify matching requests according to their verb and the target non-resource URL.
  public nonResourceRules?: NonResourcePolicyRule[];

  // `resourceRules` is a slice of ResourcePolicyRules that identify matching requests according to their verb and the target resource. At least one of `resourceRules` and `nonResourceRules` has to be non-empty.
  public resourceRules?: ResourcePolicyRule[];

  // subjects is the list of normal user, serviceaccount, or group that this rule cares about. There must be at least one member in this slice. A slice that includes both the system:authenticated and system:unauthenticated user groups matches every request. Required.
  public subjects: Subject[];

  constructor(desc: PolicyRulesWithSubjects) {
    this.nonResourceRules = desc.nonResourceRules;
    this.resourceRules = desc.resourceRules;
    this.subjects = desc.subjects;
  }
}

// PriorityLevelConfiguration represents the configuration of a priority level.
export class PriorityLevelConfiguration implements KubernetesObject {
  // APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
  public apiVersion: string;

  // Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
  public kind: string;

  // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
  public metadata: apisMetaV1.ObjectMeta;

  // `spec` is the specification of the desired behavior of a "request-priority". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
  public spec?: PriorityLevelConfigurationSpec;

  // `status` is the current status of a "request-priority". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
  public status?: PriorityLevelConfigurationStatus;

  constructor(desc: PriorityLevelConfiguration.Interface) {
    this.apiVersion = PriorityLevelConfiguration.apiVersion;
    this.kind = PriorityLevelConfiguration.kind;
    this.metadata = desc.metadata;
    this.spec = desc.spec;
    this.status = desc.status;
  }
}

export function isPriorityLevelConfiguration(o: any): o is PriorityLevelConfiguration {
  return o && o.apiVersion === PriorityLevelConfiguration.apiVersion && o.kind === PriorityLevelConfiguration.kind;
}

export namespace PriorityLevelConfiguration {
  export const apiVersion = "flowcontrol.apiserver.k8s.io/v1beta1";
  export const group = "flowcontrol.apiserver.k8s.io";
  export const version = "v1beta1";
  export const kind = "PriorityLevelConfiguration";

  // named constructs a PriorityLevelConfiguration with metadata.name set to name.
  export function named(name: string): PriorityLevelConfiguration {
    return new PriorityLevelConfiguration({metadata: {name}});
  }
  // PriorityLevelConfiguration represents the configuration of a priority level.
  export interface Interface {
    // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
    metadata: apisMetaV1.ObjectMeta;

    // `spec` is the specification of the desired behavior of a "request-priority". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
    spec?: PriorityLevelConfigurationSpec;

    // `status` is the current status of a "request-priority". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
    status?: PriorityLevelConfigurationStatus;
  }
}

// PriorityLevelConfigurationCondition defines the condition of priority level.
export class PriorityLevelConfigurationCondition {
  // `lastTransitionTime` is the last time the condition transitioned from one status to another.
  public lastTransitionTime?: apisMetaV1.Time;

  // `message` is a human-readable message indicating details about last transition.
  public message?: string;

  // `reason` is a unique, one-word, CamelCase reason for the condition's last transition.
  public reason?: string;

  // `status` is the status of the condition. Can be True, False, Unknown. Required.
  public status?: string;

  // `type` is the type of the condition. Required.
  public type?: string;
}

// PriorityLevelConfigurationList is a list of PriorityLevelConfiguration objects.
export class PriorityLevelConfigurationList {
  // APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
  public apiVersion: string;

  // `items` is a list of request-priorities.
  public items: PriorityLevelConfiguration[];

  // Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
  public kind: string;

  // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
  public metadata?: apisMetaV1.ListMeta;

  constructor(desc: PriorityLevelConfigurationList) {
    this.apiVersion = PriorityLevelConfigurationList.apiVersion;
    this.items = desc.items.map((i) => new PriorityLevelConfiguration(i));
    this.kind = PriorityLevelConfigurationList.kind;
    this.metadata = desc.metadata;
  }
}

export function isPriorityLevelConfigurationList(o: any): o is PriorityLevelConfigurationList {
  return o && o.apiVersion === PriorityLevelConfigurationList.apiVersion && o.kind === PriorityLevelConfigurationList.kind;
}

export namespace PriorityLevelConfigurationList {
  export const apiVersion = "flowcontrol.apiserver.k8s.io/v1beta1";
  export const group = "flowcontrol.apiserver.k8s.io";
  export const version = "v1beta1";
  export const kind = "PriorityLevelConfigurationList";

  // PriorityLevelConfigurationList is a list of PriorityLevelConfiguration objects.
  export interface Interface {
    // `items` is a list of request-priorities.
    items: PriorityLevelConfiguration[];

    // `metadata` is the standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
    metadata?: apisMetaV1.ListMeta;
  }
}

// PriorityLevelConfigurationReference contains information that points to the "request-priority" being used.
export class PriorityLevelConfigurationReference {
  // `name` is the name of the priority level configuration being referenced Required.
  public name: string;

  constructor(desc: PriorityLevelConfigurationReference) {
    this.name = desc.name;
  }
}

// PriorityLevelConfigurationSpec specifies the configuration of a priority level.
export class PriorityLevelConfigurationSpec {
  // `limited` specifies how requests are handled for a Limited priority level. This field must be non-empty if and only if `type` is `"Limited"`.
  public limited?: LimitedPriorityLevelConfiguration;

  // `type` indicates whether this priority level is subject to limitation on request execution.  A value of `"Exempt"` means that requests of this priority level are not subject to a limit (and thus are never queued) and do not detract from the capacity made available to other priority levels.  A value of `"Limited"` means that (a) requests of this priority level _are_ subject to limits and (b) some of the server's limited capacity is made available exclusively to this priority level. Required.
  public type: string;

  constructor(desc: PriorityLevelConfigurationSpec) {
    this.limited = desc.limited;
    this.type = desc.type;
  }
}

// PriorityLevelConfigurationStatus represents the current state of a "request-priority".
export class PriorityLevelConfigurationStatus {
  // `conditions` is the current state of "request-priority".
  public conditions?: PriorityLevelConfigurationCondition[];
}

// QueuingConfiguration holds the configuration parameters for queuing
export class QueuingConfiguration {
  // `handSize` is a small positive number that configures the shuffle sharding of requests into queues.  When enqueuing a request at this priority level the request's flow identifier (a string pair) is hashed and the hash value is used to shuffle the list of queues and deal a hand of the size specified here.  The request is put into one of the shortest queues in that hand. `handSize` must be no larger than `queues`, and should be significantly smaller (so that a few heavy flows do not saturate most of the queues).  See the user-facing documentation for more extensive guidance on setting this field.  This field has a default value of 8.
  public handSize?: number;

  // `queueLengthLimit` is the maximum number of requests allowed to be waiting in a given queue of this priority level at a time; excess requests are rejected.  This value must be positive.  If not specified, it will be defaulted to 50.
  public queueLengthLimit?: number;

  // `queues` is the number of queues for this priority level. The queues exist independently at each apiserver. The value must be positive.  Setting it to 1 effectively precludes shufflesharding and thus makes the distinguisher method of associated flow schemas irrelevant.  This field has a default value of 64.
  public queues?: number;
}

// ResourcePolicyRule is a predicate that matches some resource requests, testing the request's verb and the target resource. A ResourcePolicyRule matches a resource request if and only if: (a) at least one member of verbs matches the request, (b) at least one member of apiGroups matches the request, (c) at least one member of resources matches the request, and (d) least one member of namespaces matches the request.
export class ResourcePolicyRule {
  // `apiGroups` is a list of matching API groups and may not be empty. "*" matches all API groups and, if present, must be the only entry. Required.
  public apiGroups: string[];

  // `clusterScope` indicates whether to match requests that do not specify a namespace (which happens either because the resource is not namespaced or the request targets all namespaces). If this field is omitted or false then the `namespaces` field must contain a non-empty list.
  public clusterScope?: boolean;

  // `namespaces` is a list of target namespaces that restricts matches.  A request that specifies a target namespace matches only if either (a) this list contains that target namespace or (b) this list contains "*".  Note that "*" matches any specified namespace but does not match a request that _does not specify_ a namespace (see the `clusterScope` field for that). This list may be empty, but only if `clusterScope` is true.
  public namespaces?: string[];

  // `resources` is a list of matching resources (i.e., lowercase and plural) with, if desired, subresource.  For example, [ "services", "nodes/status" ].  This list may not be empty. "*" matches all resources and, if present, must be the only entry. Required.
  public resources: string[];

  // `verbs` is a list of matching verbs and may not be empty. "*" matches all verbs and, if present, must be the only entry. Required.
  public verbs: string[];

  constructor(desc: ResourcePolicyRule) {
    this.apiGroups = desc.apiGroups;
    this.clusterScope = desc.clusterScope;
    this.namespaces = desc.namespaces;
    this.resources = desc.resources;
    this.verbs = desc.verbs;
  }
}

// ServiceAccountSubject holds detailed information for service-account-kind subject.
export class ServiceAccountSubject {
  // `name` is the name of matching ServiceAccount objects, or "*" to match regardless of name. Required.
  public name: string;

  // `namespace` is the namespace of matching ServiceAccount objects. Required.
  public namespace: string;

  constructor(desc: ServiceAccountSubject) {
    this.name = desc.name;
    this.namespace = desc.namespace;
  }
}

// Subject matches the originator of a request, as identified by the request authentication system. There are three ways of matching an originator; by user, group, or service account.
export class Subject {
  public group?: GroupSubject;

  // Required
  public kind: string;

  public serviceAccount?: ServiceAccountSubject;

  public user?: UserSubject;

  constructor(desc: Subject) {
    this.group = desc.group;
    this.kind = desc.kind;
    this.serviceAccount = desc.serviceAccount;
    this.user = desc.user;
  }
}

// UserSubject holds detailed information for user-kind subject.
export class UserSubject {
  // `name` is the username that matches, or "*" to match all usernames. Required.
  public name: string;

  constructor(desc: UserSubject) {
    this.name = desc.name;
  }
}